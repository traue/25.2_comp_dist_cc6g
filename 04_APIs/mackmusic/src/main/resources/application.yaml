server:
  port: 8081

spring:
  application:
    name: mackmusic_06g

  datasource:
    url: jdbc:mariadb://localhost:3306/mackmusic
    username: user
    password: Senha123
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
