spring:
  application:
    name: mackmusic
  datasource:
    url: jdbc:mariadb://localhost:3306/music_recommender
    username: user
    password: Senha123
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html
  openapi:
    info:
      title: MackMusic API
      version: v0.0.1
      description: API para o sistema de gerenciamento de músicas MackMusic.